/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'spotify-nextjs-nestjs-server.onrender.com'],
  },
}

module.exports = nextConfig
